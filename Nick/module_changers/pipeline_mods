# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# File sourced by primary_processing or any sub units if run independently to source modules or add links/shortcuts that are needed for analysis

export PATH=$PATH:$HOME/bin/
export BLASTN="blastn"
export MAKEBLASTDB="makeblastdb"
export PERL5LIB=~/lib/perl5

module load BBMap/38.26
module load trimmomatic/0.36
module load ncbi-blast+/LATEST
module load Python/3.5.2
module load perl/5.22.1
